server:
  port: 9090

spring:
  application:
    name: emailmanager

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/email_manager
    username: 'root'
    password: 'rootroot'

  jpa:
    show-sql: true
    generate-ddl: true

  mvc:
    pathmatch:
      matching-strategy: path_pattern_parser